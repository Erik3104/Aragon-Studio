<Window x:Class="AragonStudio.UI.CatalogoDeModelosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Catálogo de Modelos"
        Height="420" Width="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        Background="#FAFAFA">

    <Border CornerRadius="10"
            Background="White"
            Padding="20"
            Margin="15"
            BorderBrush="#DDDDDD"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Título -->
                <RowDefinition Height="*"/>
                <!-- Lista -->
                <RowDefinition Height="Auto"/>
                <!-- Botones -->
                <RowDefinition Height="Auto"/>
                <!-- Logo -->
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Text="Catálogo de Modelos del Proyecto"
                       Grid.Row="0"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#222"
                       Margin="0,0,0,15"/>

            <!-- Lista de modelos -->
            <ListView x:Name="ListaModelos"
                      Grid.Row="1"
                      Margin="10"
                      BorderBrush="#CCCCCC"
                      BorderThickness="1"
                      Background="#FDFDFD">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Nombre del Modelo" DisplayMemberBinding="{Binding Nombre}" Width="220"/>
                        <GridViewColumn Header="Categoría" DisplayMemberBinding="{Binding Categoria}" Width="150"/>
                        <GridViewColumn Header="Fecha" DisplayMemberBinding="{Binding Fecha}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- Botones -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,10">

                <Button Content="Actualizar lista"
                        Width="160" Height="40"
                        FontSize="14"
                        Margin="0,0,10,0"
                        Click="Actualizar_Click"/>

                <Button Content="Abrir modelo"
                        Width="160" Height="40"
                        FontSize="14"
                        Margin="0,0,10,0"
                        Click="Abrir_Click"/>

                <Button Content="Cerrar"
                        Width="160" Height="40"
                        FontSize="14"
                        Click="Cerrar_Click"/>
            </StackPanel>

            <!-- Logo -->
            <Image Grid.Row="3"
                   Source="pack://application:,,,/AragonStudio;component/Resources/Icons/Logo.ico"
                   Width="64" Height="64"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,0"/>
        </Grid>
    </Border>
</Window>
