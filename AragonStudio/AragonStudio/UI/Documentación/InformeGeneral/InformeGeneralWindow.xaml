<Window x:Class="AragonStudio.UI.InformeGeneralWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Informe General del Proyecto"
        Height="360" Width="540"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        Background="#FAFAFA">

    <Border CornerRadius="10"
            Background="White"
            Padding="20"
            Margin="15"
            BorderBrush="#DDDDDD"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Título -->
                <RowDefinition Height="*"/>
                <!-- Descripción -->
                <RowDefinition Height="Auto"/>
                <!-- Botones -->
                <RowDefinition Height="Auto"/>
                <!-- Logo -->
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Text="Generador de Informe General"
                       Grid.Row="0"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#222"
                       Margin="0,0,0,15"/>

            <!-- Descripción -->
            <TextBlock Text="Crea un informe detallado con información general del proyecto Revit: niveles, vistas, elementos y cantidades."
                       Grid.Row="1"
                       FontSize="14"
                       Foreground="#555"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="10,0,10,10"/>

            <!-- Botones -->
            <StackPanel Grid.Row="2"
                        Orientation="Vertical"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,10">

                <Button Content="Analizar modelo"
                        Width="200" Height="40"
                        FontSize="14"
                        Margin="0,0,0,10"
                        Click="AnalizarModelo_Click"/>

                <Button Content="Generar informe"
                        Width="200" Height="40"
                        FontSize="14"
                        Margin="0,0,0,10"
                        Click="GenerarInforme_Click"/>

                <Button Content="Cancelar"
                        Width="200" Height="40"
                        FontSize="14"
                        Click="Cancelar_Click"/>
            </StackPanel>

            <!-- Logo -->
            <Image Grid.Row="3"
                   Source="pack://application:,,,/AragonStudio;component/Resources/Icons/Logo.ico"
                   Width="64" Height="64"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,0"/>
        </Grid>
    </Border>
</Window>
