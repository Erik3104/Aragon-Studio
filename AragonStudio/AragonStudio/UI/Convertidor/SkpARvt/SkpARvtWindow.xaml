<Window x:Class="AragonStudio.UI.SkpARvtWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Convertidor SKP a Revit"
        Height="300" Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        Background="#FAFAFA">

    <Border CornerRadius="10"
            Background="White"
            Padding="20"
            Margin="15"
            BorderBrush="#DDDDDD"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Título -->
                <RowDefinition Height="*"/>
                <!-- Descripción -->
                <RowDefinition Height="Auto"/>
                <!-- Botones -->
                <RowDefinition Height="Auto"/>
                <!-- Logo -->
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Text="Convertidor SketchUp a Revit"
                       Grid.Row="0"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#222"
                       Margin="0,0,0,15"/>

            <!-- Descripción -->
            <TextBlock Text="Convierte archivos de SketchUp (.SKP) al entorno de Revit manteniendo geometrías y materiales."
                       Grid.Row="1"
                       FontSize="14"
                       Foreground="#555"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="10,0,10,10"/>

            <!-- Botones -->
            <StackPanel Grid.Row="2"
                        Orientation="Vertical"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,10">

                <Button Content="Seleccionar archivo SKP"
                        Width="200" Height="40"
                        FontSize="14"
                        Margin="0,0,0,10"
                        Click="SeleccionarArchivo_Click"/>

                <Button Content="Convertir a Revit"
                        Width="200" Height="40"
                        FontSize="14"
                        Margin="0,0,0,10"
                        Click="Convertir_Click"/>

                <Button Content="Cancelar"
                        Width="200" Height="40"
                        FontSize="14"
                        Click="Cancelar_Click"/>
            </StackPanel>

            <!-- Logo -->
            <Image Grid.Row="3"
                   Source="pack://application:,,,/AragonStudio;component/Resources/Icons/Logo.ico"
                   Width="64" Height="64"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,0"/>
        </Grid>
    </Border>
</Window>
